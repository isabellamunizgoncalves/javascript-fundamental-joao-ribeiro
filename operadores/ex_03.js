let factor = 5;

let tabuada =[
    factor * 1,
    factor * 2,
    factor * 3,
    factor * 4,
    factor * 5,
    factor * 6,
    factor * 7,
    factor * 8,
    factor * 9,
    factor * 10,
];

console.table(tabuada);

