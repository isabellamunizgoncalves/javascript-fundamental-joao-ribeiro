//array de strings
let names = ['Isa', 'Rique', 'Mamusca'];

//buscar o primeiro elemento do array
console.log(names[0]);

//os índices do array começam em 0 - zero 
//então, se um array tem 3 elementos, o último eleemento estará no índice 2
console.log(names[2])//Mamusca

//se tentarmos acessar um elemento que não existe, teremos o undefined
console.log(names[3]);